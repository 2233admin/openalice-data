# Surface: Workspaces

工作区是数据源页中的平级分类和组合式数据预配装，不是新的一级产品页，也不是启动方案。

- 工作区保存明确的数据源成员、成员顺序和已有自动切换/批量策略。
- 同一原生数据源可属于多个工作区；工作区可为空后再加入成员。
- 移除成员或删除工作区只删除组织关系，不删除 Provider、凭证或原生数据源。
- 启动方案只引用环境中的服务和可启动前端，不保存工作区或数据源。
- 现有 `workspace-*` 存储和路由名称可作为迁移实现细节；正常入口仍是数据源。
