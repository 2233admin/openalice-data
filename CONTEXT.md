# OpenAlice Data

OpenAlice Data 是 Open Data Platform Desktop 的中文化、人性化 fork。它保留原版的数据源、凭证、扩展、运行环境和路由能力，在其上增加工作区、组合使用和更清晰的任务体验，而不是重新发明这些概念。

正常一级导航只有“首页 / 数据源 / 环境与扩展”。原版 Backends、Environments、Extensions、Jupyter、Logs 以及单独的可视化前端页面都被吸收到这三个用户任务中；旧 URL 只能用于迁移安全，不能继续作为正常导航。

## Language

**数据源**：
ODP/OpenBB 原生提供的数据来源。它可以由原版 API Keys 界面中的凭证启用，也可以是不需要凭证的内置数据源；OAD 不把每个 Provider × Dataset/API 重新定义成独立数据源。
_Avoid_: Provider 能力行、连接对象、Dataset/API 数据源

**API Key 数据源**：
通过原版 API Keys 能力配置凭证后可使用的原生数据源。一个数据源可以包含 API Key、Secret、Account ID 等多个必需凭证字段，但在主列表中仍只占一行，字段集中在编辑操作中。OAD 保留原版紧凑的凭证组件和交互，不增加新的 Provider 或“凭证配置档”用户概念。
_Avoid_: Provider 连接、凭证卡片、把多个凭证字段拆成多个数据源

**数据源/工作区管理**：
唯一的数据源管理页面，不与原版 API Keys 页面并存。左侧是平级、紧凑的工作区分类，右侧沿用原版 API Keys 的高密度组件和底层 ODP 凭证能力，并在此直接完成搜索、添加、编辑和删除；一个 API Key 数据源占一行，内置数据源也各占一行并统一排在列表底部，具体 API/能力只在详情中查看。勾选一个或多个数据源后可加入工作区，同一数据源可属于多个工作区。
新添加或批量导入的数据源立即出现在“全部数据源”；尚未加入任何工作区的来源显示为“未分类”。
原版 `/api-keys` 可仅为旧链接迁移保留，不再作为正常导航或第二套管理界面。
_Avoid_: 首页内编辑、Provider × API 大卡片、双重 API Keys/数据源页面、用新的低密度组件重做 API Keys

**内置数据源**：
不需要用户配置 API Key 即可使用的原生数据源。
_Avoid_: 无凭证 Provider、虚拟 API Key

**工作区**：
类似 FPS 游戏的“预配装”：用户预先保存的一组原生数据源及其使用策略。它既用于分类和组织，也是可直接选择的组合式数据源。同一数据源可以属于多个工作区；选择工作区后可以启动数据可视化前端并使用其中多个数据源。
工作区可以先创建为空再逐步加入数据源，也可以由当前勾选的数据源直接创建；所有工作区保持平级，不做嵌套。
从工作区移除成员或删除整个工作区只删除组织关系，始终保留原生数据源和 API Keys；只有用户在原生数据源/API Keys 管理中明确删除凭证时才删除它。
_Avoid_: 文件夹、嵌套分类、强制至少两个成员、Workspace 存储对象

**自动路由**：
工作区成员具有明确顺序，可在管理页拖动调整；自动路由按此优先级尝试。ODP 原生选择和执行仍是基础，工作区只补充成员范围以及真实请求失败后的下一成员重试。
_Avoid_: 前端自建路由、按名称猜测回退、隐藏优先级

工作区不区分跨数据商切换与同一数据商的多个账号连接；它们都是普通成员，由同一成员顺序和模式处理，不为此创建额外的用户对象。

**批量取数**：
工作区向所有支持所需能力的成员请求数据，保留来源标识并把结果分别交给可视化前端。OAD 不自动合并结果；展示和进一步组合由可视化前端负责。它与故障回退是两种明确模式，不能隐式混用。
_Avoid_: 自动合并、隐式回退

工作区保存自动路由或批量取数等默认使用策略；首页不增加单独的模式控件，选择工作区即选择其默认使用策略。

**扩展**：
安装到 OpenBB 环境中的可视化前端、研究工具或其他用户可识别的运行组件。可视化前端是具有可启动入口的一类扩展，不建立独立的前端配置对象或管理页。Provider、Router 即使在 ODP 底层以 Python 扩展包实现，在产品界面中也属于数据源能力，不在“环境与扩展”中重复暴露或管理。
扩展管理遵循“安装单元即管理单元”：OpenBB 自带的标准运行组件合并为一项不可删除的“OpenBB 标准扩展”；用户后来主动安装的可视化前端、研究工具等扩展各占一行。同一扩展带入的 Python 依赖、OpenBB 核心包和内部组件默认隐藏，不把原始包清单当成产品界面；需要诊断时才在扩展详情中查看技术依赖。
_Avoid_: 独立扩展市场、与环境分离的安装页、重复管理 Provider/Router、添加数据源、逐包平铺 Python 依赖、让用户判断内部包能否删除

**环境与扩展**：
用户选择、创建和维护 OpenBB 运行环境，并在同一上下文中管理该环境的启动方案、原生 Backend 服务、扩展与研究工具。原版 Environment 和 Backends 页面不再作为两个独立产品入口。
环境继续使用 ODP 原生的受管 Conda 能力：新建环境沿用名称、Python 版本和安装扩展的底层流程，导入继续支持 YAML、`pyproject.toml` 和 `requirements.txt`。界面简化步骤和术语，但第一阶段不新增复制环境、环境模板或接入任意外部 Python 环境；环境管理中唯一新增的运行组织概念是“启动方案”。
_Avoid_: Environments、Extensions 双页面、要求用户理解 Conda 后才能开始、复制环境、环境模板、外部环境接入

**ODP 控制面**：
OpenAlice 继承的底层服务、环境、凭证、Jupyter 和日志能力。它们被吸收到对应的用户任务中，不再以 `ODP` 分组、原版页面名或独立控制面出现在正常界面；技术归属只在关于/技术信息中说明。
_Avoid_: ODP 导航分组、Backends、Environments、API Keys、Logs 产品页、Advanced、通用 Settings

**Python Notebook（Jupyter）**：
面向量化研究和回测的 Python 笔记本工具，归入“环境与扩展”，不占一级导航，也不进入首页的运行服务选择。用户在目标环境中启动、打开或停止 Notebook，日志附着在对应实例上。
_Avoid_: 一级 Jupyter 页面、首页服务候选项、Jupyter Logs 独立页面、仅用技术名词而不说明用途

**上下文日志**：
由服务、Python Notebook 或扩展任务产生并附着在该对象上的运行证据。日志使用原生查看能力，但不构成一级 Logs 产品页。
_Avoid_: 全局 Logs 导航、脱离对象的错误档案

**服务启动与执行**：
启动、停止、状态、凭证安全和能力注册由 ODP 原生能力负责。OAD/OAP 选择工作区、数据源和可视化前端，并调用既有 ODP 动作；工作区只在 ODP Provider 执行边界上补充成员范围和运行时故障切换，不复制服务状态机或创建查询引擎。
_Avoid_: 前端凭证桥、第二套服务状态机、查询引擎、绕过 ODP 原生启停能力

选择工作区启动时，该工作区定义本次启动可使用的数据源范围：只有工作区成员参与使用和自动路由。其他全局数据源和 API Keys 保持原样，但不参与本次启动。运行时故障切换在成员中处理无效凭证、限流或 Provider 不可用等来源故障；它不把无数据、用户输入错误或不兼容能力误判为切换理由。

第一版只有一个当前生效工作区。再次选择另一个工作区并 `Start` 会切换当前预配装；不为不同前端启动多个隔离的 ODP Backend 实例。

**启动方案**：
保存在某个运行环境中的统一启动预配装。方案包含一张启动项清单，服务和可视化前端只是启动项的不同分类，而不是主体与附属动作；同一方案可以选择多个服务和多个前端。启动方案不保存数据源或工作区；工作区仍是独立的数据预配装。一个环境可以拥有多个启动方案，环境本身不能被整体启动。
方案引用已有服务和已有前端；命令、端口、环境变量、工作目录等仍属于服务配置，不复制进方案。执行时先启动清单中的服务，再打开清单中的前端；部分服务失败不阻止其他启动项继续执行。
启动方案不接管或重做 ODP 安装初始化；默认环境、OpenBB API、OpenBB MCP 及其他原生对象继续由原版流程创建，方案只编排已经存在的能力。
原版 Backend 的单服务“启动应用时自动运行”不再作为正常设置。迁移时，已启用自动启动的服务加入所属环境的默认启动方案并关闭旧自动启动行为；打开 OpenAlice 仍等待用户在首页明确点击 `Start`。
_Avoid_: 启动整个环境、把工作区复制进启动方案、把启动方案缩减为纯服务清单、只允许一个前端、把前端降为方案外的附属选择

**首页**：
用户在统一启动区选择运行环境、该环境中的启动方案和数据源/工作区，然后点击一个 `Start`。选中方案后必须看得到本次具体启动项；用户仍可调整本次实际启动的服务和前端。首页只做选择和启动，不在原地创建或编辑对象；各选择器提供对应管理入口。
_Avoid_: 大卡片、隐藏服务选择、内嵌管理表单、Query、能力注册表、安全配置向导、重复实现 ODP 启停

环境、该环境下的启动方案、数据源/工作区与 Start 属于同一紧凑启动区。默认只显示三个选择器和方案摘要；摘要分别说明将启动的服务和前端数量。“调整本次启动”才展开按类别分组的统一启动项清单，临时调整不覆盖已保存方案。运行服务显示名称、所属环境和当前状态；方案可以预选启动项，但用户可以取消、改选或选择零个服务。首页恢复上次的环境、方案、工作区和本次启动项调整，但不自动启动。

`Start` 执行用户本次明确选择的全部启动项；零服务可以只打开一个或多个前端，零前端也可以只启动服务。

**本次启动**：
一次 Start 产生的运行批次，记录本次新启动、复用、失败和重试的服务。用户可以停止本次新启动的服务、停止全部服务，或明确选择要停止的服务；此前已运行且仅被复用的服务不会被“停止本次启动”误停。
_Avoid_: Session 产品、隐式停止、把已运行服务当成本次新启动

**运行服务**：
配置在某个环境中的可启动进程，例如 OpenBB API、MCP 或自定义 Backend。它不是可视化前端，也不占一级导航；配置与手动控制属于环境，本次启动选择直接出现在首页。
_Avoid_: Backend 一级产品页、隐藏本次服务选择、把服务与前端合并

已运行的所选服务在 Start 时直接复用；服务支持独立重启，失败后支持独立重试。部分服务失败不阻止打开前端，但必须保留失败状态和对应日志入口。

**前端**：
连接 ODP Hub/Backend 的数据可视化扩展，例如 OpenBB 的数据可视化工作区或安装到环境中的其他可视化前端。REST、MCP、Python Notebook 等内部运行能力不是这里所说的前端。
_Avoid_: 查询页、REST、MCP、Python Notebook、内部能力注册表

**可视化前端启动项**：
原版 ODP 没有独立的前端清单或管理产品；Backend 启动后只提供连接 OpenBB Workspace 的引导。OAD 不建立一级“可视化前端管理”页面，也不新增名称/URL前端配置；前端通过扩展能力安装和删除，启动方案只从当前环境已安装的前端扩展中选择，并允许同一方案包含多个前端。
_Avoid_: 一级可视化前端管理、首页内编辑、自定义前端 URL 对象、连接诊断表、运行状态仪表盘、脱离扩展安装前端

原版 Backend 启动后给出的 OpenBB Workspace 英文连接步骤应改成简短、中文、可操作的引导；能够由 OAD 完成的打开、地址填充或复制动作自动完成，目标前端不提供接口的步骤才交给用户。

OpenBB Workspace 是不可删除的内置前端扩展。其他前端通过扩展安装能力进入当前环境；第一阶段只改善入口、中文引导和管理体验，不发明新的前端添加协议。
