# Repository Agent Guide

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues for `2233admin/openalice-data`. Use `gh` for tracker operations. See `docs/agents/issue-tracker.md`.

### Domain docs

This is a single-context repository. Read `CONTEXT.md` and relevant decisions under `docs/adr/` when they exist. See `docs/agents/domain.md`.
